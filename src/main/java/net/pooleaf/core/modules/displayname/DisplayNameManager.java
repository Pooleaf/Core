package net.pooleaf.core.modules.displayname;

import net.pooleaf.core.modules.support.common.manager.AbstractManager;

import java.util.UUID;

public class DisplayNameManager extends AbstractManager<UUID, String> {

    @Override
    public String load(UUID key) {
        // TODO 불러오기 구현
        return super.load(key);
    }

}
